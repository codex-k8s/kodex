import {ResourceKind} from './ResourceKind';
import {ScheduleCalendar} from './ScheduleCalendar';
import {ScheduleOverlapPolicy} from './ScheduleOverlapPolicy';
import {ScheduleMisfirePolicy} from './ScheduleMisfirePolicy';
import {ScheduleDeliveryPolicy} from './ScheduleDeliveryPolicy';
import {ScheduleSessionPolicy} from './ScheduleSessionPolicy';
import {ScheduleNotificationPolicy} from './ScheduleNotificationPolicy';
import {ScheduleTargetType} from './ScheduleTargetType';
import {ConfigurationOwnershipProjection} from './ConfigurationOwnershipProjection';
interface IntervalScheduleProjection {
  targetResourceId: string;
  targetKind: ResourceKind;
  targetVersion: number;
  cron?: string;
  intervalSeconds: number;
  timezone: string;
  nextRunAt: string;
  calendar: ScheduleCalendar;
  overlapPolicy: ScheduleOverlapPolicy;
  misfirePolicy: ScheduleMisfirePolicy;
  misfireGraceSeconds: number;
  deliveryPolicy: ScheduleDeliveryPolicy;
  maximumAttempts: number;
  initialBackoffSeconds: number;
  maximumBackoffSeconds: number;
  deadLetterAfterSeconds: number;
  promptProfileId: string;
  promptRevision: number;
  sessionPolicy: ScheduleSessionPolicy;
  roomId?: string;
  notificationPolicy: ScheduleNotificationPolicy;
  maximumExecutionSeconds: number;
  coalesce: boolean;
  runtimeRevisionId: string;
  targetType: ScheduleTargetType;
  playbookRef?: string;
  playbookVersion?: number;
  promptArtifactId: string;
  executionSessionId?: string;
  ownership: ConfigurationOwnershipProjection;
}
export { IntervalScheduleProjection };