-- name: slot__insert :exec
INSERT INTO runtime_manager_slots (
    id,
    slot_key,
    status,
    runtime_mode,
    is_prewarmed,
    fleet_scope_id,
    cluster_id,
    namespace_name,
    agent_run_id,
    project_id,
    repository_ids_json,
    runtime_profile,
    fingerprint,
    lease_owner,
    lease_until,
    last_error_code,
    last_error_message,
    version,
    created_at,
    updated_at
) VALUES (
    @id,
    @slot_key,
    @status,
    @runtime_mode,
    @is_prewarmed,
    @fleet_scope_id::uuid,
    @cluster_id::uuid,
    @namespace_name,
    @agent_run_id::uuid,
    @project_id::uuid,
    @repository_ids_json::jsonb,
    @runtime_profile,
    @fingerprint,
    @lease_owner,
    @lease_until::timestamptz,
    @last_error_code,
    @last_error_message,
    @version,
    @created_at,
    @updated_at
);
