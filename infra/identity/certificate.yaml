apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sso-public-tls
spec:
  secretName: sso-public-tls
  issuerRef:
    kind: ClusterIssuer
    name: __KODEX_CLUSTER_ISSUER__
  dnsNames:
    - __KODEX_OIDC_HOST__
