// Только локальный controlled transport; никаких vendor calls.
import { createHash } from 'node:crypto';
const emailInputDigest = (input) => createHash('sha256').update(JSON.stringify(Object.fromEntries(Object.keys(input).sort().map((key) => [key, input[key]]))).replace(/[<>&\u2028\u2029]/g, (c) => `\\u${c.charCodeAt(0).toString(16).padStart(4, '0')}`)).digest('hex');
const hash = 'a'.repeat(64);
const schema = JSON.stringify({ type: 'object', additionalProperties: false, properties: { to: { type: 'string' }, subject: { type: 'string' }, body_text: { type: 'string' } }, required: ['to', 'subject', 'body_text'] });
export const emailAgentFixture = { version: 1, prefix: 'mvp1371-local', projectRef: 'prj_fixture', agentRef: 'agt_fixture', connectionRef: 'int_fixture', configurationRef: 'cfg_fixture', mailboxRevisionRef: 'rev_fixture', accountRef: 'pacc_fixture', model: 'fixture-model', defaultReasoningEffort: 'low', capabilityKey: 'email.message.send', input: { to: 'qa@example.invalid', subject: 'private marker', body_text: 'private <input> & Пример' } };
export function emailAgentTransport(mode = '', options = {}) {
  const f = emailAgentFixture;
  const combined=options.combined===true; const nonce=options.nonce??'1'.repeat(32);
  const image=`pull.fixture.invalid/kodex/roles@sha256:${hash}`;
  const proof=Buffer.from(JSON.stringify({schema:'kodex.workspace-acceptance.v1',nonce,uid:10002,checks:Object.fromEntries(['create','read','atomicReplace','readReplacement','delete','input','source','context','skills','memory','credential','symlink','traversal','outside'].map(k=>[k,true])),resultSHA256:createHash('sha256').update(`mvp-workspace:${nonce}:replacement\n`).digest('hex')}));
  const provenance=Buffer.from(JSON.stringify({schema:'kodex.workspace-write-result.v1',runtime_revision_ref:'rtvr_fixture',runtime_revision_version:1,runtime_revision_digest:hash,attempt:1,execution_binding_digest:'e'.repeat(64)}));
  const artifacts=[['mvp-workspace-proof.json',proof],['mvp-workspace-result.txt',Buffer.from(`mvp-workspace:${nonce}:replacement\n`)],['workspace-write-result.json',provenance]];
  const inputSchema = mode === 'narrowed' ? schema.replace('\"type\":\"string\"', '\"type\":\"string\",\"maxLength\":1') : schema;
  const config = { ref: 'rcfg_fixture', version: 1, agentRef: f.agentRef, digest: hash, model: f.model, providerPolicy: { mode: 'FIXED', ref: 'policy_fixture', version: 1, digest: hash, accountCandidates: [{ accountRef: f.accountRef, providerDefinitionKey: 'openai-codex', defaultReasoningEffort: 'low', catalogRevision: `mcat_${hash}`, catalogDigest: hash }] } };
  const grant = { ref: 'grant_fixture', version: 1, agentRef: f.agentRef, capabilityKey: f.capabilityKey, enabled: true, inputSchema, inputSchemaSha256: createHash('sha256').update(inputSchema).digest('hex') };
  const terminal = ['FAILED', 'REJECTED', 'CANCELLED', 'UNKNOWN_OUTCOME'].includes(mode) ? mode : 'SUCCEEDED';
  const run = { ref: 'run_fixture', projectRef: f.projectRef, sessionRef: 'ses_fixture', attempt: mode === 'attempt' ? 2 : 1, target: { ref: f.agentRef, type: 'AGENT' }, state: mode === 'gate' ? 'WAITING_HUMAN' : mode === 'pending' ? 'RUNNING' : 'SUCCEEDED', artifactRefs: combined ? artifacts.map((_,i)=>`artifact_${i}`) : [], lastEventSequence: ['gate', 'pending'].includes(mode) ? 0 : (mode === 'multiple' ? 2 : 1)+(combined?1:0), gateRefs: ['gate_fixture'] };
  return (path, method = 'GET') => {
    if(combined && path.includes('/role-image-recipes/')) return {recipe:{ref:'recipe_fixture',version:1,generation:1,managedLineage:{managedBy:'UI',revisionRef:'mrev_fixture'},promotedImageReady:true,activeImageArtifactRef:'imgart_fixture'},builds:[{ref:'imgbld_fixture',recipeRef:'recipe_fixture',recipeGeneration:1,configurationRevisionRef:'mrev_fixture',stage:'COMPLETED'}],activeArtifact:{ref:'imgart_fixture',recipeRef:'recipe_fixture',recipeGeneration:1,admissionVerdict:'ACCEPTED',manifestDigest:`sha256:${hash}`,promotedReference:image,promotionReceiptSha256:hash,provenanceSha256:hash,sbomSha256:hash,vulnerabilityEvidenceSha256:hash,promotedAt:'2026-09-09T01:00:00Z'}};
    if(combined && path.startsWith('/api/v1/artifacts/artifact_')) {const index=Number(path.split('artifact_')[1].split('/')[0]);const [fileName,bytes]=artifacts[index];if(path.endsWith('/content'))return bytes; return {ref:`artifact_${index}`,fileName,runRef:run.ref,projectRef:f.projectRef,sessionRef:run.sessionRef,source:'AGENT_RESULT',lifecycleState:'ACTIVE',attempt:1,scanState:mode==='partial-workspace'?'FAILED':'CLEAN',sizeBytes:bytes.length,digest:`sha256:${createHash('sha256').update(bytes).digest('hex')}`};}
    if (method === 'POST' && path === '/api/v1/runs') return { run };
    if (method === 'POST' && path === '/api/v1/projects/prj_fixture/agents') return { ref: f.agentRef, projectRef: f.projectRef, version: 1 };
    if (path === '/api/v1/projects/prj_fixture') return { ref: f.projectRef };
    if (path === '/api/v1/agents/agt_fixture') return { ref: f.agentRef, projectRef: mode === 'foreign' ? 'prj_other' : f.projectRef, enabled: mode !== 'disabled', system: false, state: 'READY', version: mode === 'stale' ? 2 : 1 };
    if (path.endsWith('/runtime-configuration')) return { agentVersion: mode === 'stale' ? 2 : 1, configuration: config, publishedOverlay: { ref: 'ovl_fixture', digest: hash }, environmentBinding: { ref:'binding_fixture',digest:hash,agentRef: f.agentRef, environmentRef: 'env_fixture', versionRef: 'envv_fixture', version: 1 }, environment: { ref: 'env_fixture', projectRef: f.projectRef, ready: true, currentVersion: { ref: 'envv_fixture', image: {artifactRef:'imgart_fixture', reference:combined?image:`registry.fixture.invalid/image@sha256:${hash}` } } } };
    if (path.startsWith('/api/v1/model-capabilities?')) return { catalogRevision: `mcat_${hash}`, catalogDigest: mode === 'catalog' ? 'b'.repeat(64) : hash, items: [{ id: f.model, providerDefinitionKey: 'openai-codex', available: true, eligibleProviderAccountRefs: [f.accountRef], reasoningEfforts: ['low'] }] };
    if (path.startsWith('/api/v1/provider-accounts/')) return { ref: f.accountRef, version: 1, ready: true, enabled: true, usage: { allowedToSubmit: mode !== 'account', agentVersion: mode === 'stale' ? 2 : 1, runtimeConfigurationRef: config.ref, runtimeConfigurationDigest: hash } };
    if (path === '/api/v1/integration-connections/int_fixture') return { ref: f.connectionRef, version: 1, definitionKey: 'email', definitionVersion: '1.4.1', definitionDigest: hash, state: 'CONNECTED', grants: mode === 'missing-grant' ? [] : [grant] };
    if (path.includes('/email-mailbox/configuration?')) return { connectionRef: f.connectionRef, connectionVersion: 1, mailboxRef: 'mail_fixture', configuration: { ref: f.configurationRef }, revision: { ref: f.mailboxRevisionRef, state: 'PUBLISHED', digest: hash }, boundRevisionRef: f.mailboxRevisionRef, publication: { ref: 'pub_fixture', state: mode === 'publication' ? 'SUPERSEDED' : 'READY', configurationRevisionRef: f.mailboxRevisionRef, revision: 3, digest: hash } };
    if (path.includes('/effective-capabilities?')) return { agentRef: f.agentRef, projectRef: f.projectRef, agentVersion: mode === 'stale' ? 2 : 1, runtimeConfigurationRef: config.ref, runtimeConfigurationVersion: 1, environmentVersionRef: 'envv_fixture', runtimeReady: true, digest: hash, items: [...(combined?[{key:'platform.artifact.manage',effective:mode!=='no-artifact'}]:[]),{ key: f.capabilityKey, connectionRef: f.connectionRef, connectionVersion: 1, grantRef: grant.ref, grantVersion: 1, effective: mode !== 'ineffective', definitionDigest: hash }] };
    if (path === '/api/v1/runs/run_fixture') return run;
    if (path === '/api/v1/runs/run_fixture/runtime-revision-diff') return { current: { ref: 'rtvr_fixture', version: 1, runRef: run.ref, sessionRef: run.sessionRef, attempt: 1, turnRef: 'turn_fixture', revisionDigest: hash }, changes: [...(combined?[{component:'IMAGE',current:{digest:mode==='wrong-image'?'b'.repeat(64):hash}}]:[]),{ component: 'MODEL', current: { ref: f.model } }, { component: 'RUNTIME_CONFIGURATION', current: { ref: config.ref, version: 1, digest: mode === 'runtime-drift' ? 'b'.repeat(64) : hash } }, { component: 'PROVIDER_POLICY', current: { ref: config.providerPolicy.ref, version: 1, digest: hash } }] };
    if (path === '/api/v1/runs/run_fixture/graph') return { runRef: run.ref, nodes: [{ ref: 'node_fixture', runRef: run.ref, agentRef: mode === 'foreign-node' ? 'agt_other' : f.agentRef, turnRef: 'turn_fixture', attempt: 1 }] };
    if (path.includes('/events?')) {
      const result = { version: 1, invocationRef: mode === 'malformed-ref' ? 'inv/private' : 'inv_fixture', state: terminal, inputSHA256: mode === 'input-changed' ? 'b'.repeat(64) : emailInputDigest(f.input) };
      const event = { ref: 'evt_fixture', runRef: mode === 'foreign-event' ? 'run_other' : run.ref, nodeRef: 'node_fixture', sequence: 1, type: 'TOOL_CALL_RECORDED', toolCall: { ref: 'tool_fixture', tool: 'invoke_integration', state: 'SUCCEEDED', safeParameters: { connection_ref: f.connectionRef, capability_key: f.capabilityKey }, capabilityRef: f.capabilityKey, grantRef: mode === 'wrong-grant' ? 'grant_other' : grant.ref, auditRef: 'audit_fixture', safeResult: mode === 'old-reader' ? 'invoke_integration:completed' : JSON.stringify(result) } };
      const calls=mode==='multiple'?[event,{...event,ref:'evt_second',sequence:2}]:[event];
      return {items:combined?[{ref:'evt_shell',runRef:run.ref,sequence:1,type:'TOOL_CALL_RECORDED',toolCall:{tool:'CODEX_SHELL',state:'SUCCEEDED',safeParameters:{source:'AGENT',cwd_scope:'WORKSPACE',exit_code:'ZERO'}}},...calls.map(e=>({...e,sequence:e.sequence+1}))]:calls};
    }
    if (path === '/api/v1/integration-invocations/inv_fixture/email-effect-receipt') return { receipt: { ref: 'receipt_fixture', version: 1, invocationRef: 'inv_fixture', connectionRef: mode === 'foreign-receipt' ? 'int_other' : f.connectionRef, projectRef: f.projectRef, mailboxRef: 'mail_fixture', configurationRevision: 3, outcome: mode === 'unknown-receipt' || mode === 'UNKNOWN_OUTCOME' ? 'UNKNOWN_OUTCOME' : 'EFFECT_CONFIRMED', externalReceiptDigest: hash, semanticInputDigest: hash } };
    throw new Error('FIXTURE_ROUTE_UNEXPECTED');
  };
}
