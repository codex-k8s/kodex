// Package stackinventory loads and resolves the repository root services.yaml.
//
// The root services.yaml is the platform stack inventory used by bootstrap,
// rendering and install tooling. It is not the project services.yaml policy
// imported by project-catalog for user repositories.
package stackinventory
