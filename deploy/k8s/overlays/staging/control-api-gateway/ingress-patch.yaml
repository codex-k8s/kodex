apiVersion: networking.k8s.io/v1
kind: Ingress
metadata: {name: control-api-gateway}
spec:
  tls:
    - hosts: [control-api.staging.mattercodex.local]
  rules:
    - host: control-api.staging.mattercodex.local
