apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mattercodex-system
resources:
  - ../../../base/control-plane
patches:
  - path: deployment-patch.yaml
labels:
  - pairs:
      mattercodex.dev/environment: staging
    includeSelectors: false
    includeTemplates: true
