apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kodex-system
resources:
  - ../../../base/control-plane
patches:
  - path: deployment-patch.yaml
  - path: broker-bootstrap-job-patch.yaml
labels:
  - pairs:
      kodex.dev/environment: staging
    includeSelectors: false
    includeTemplates: true
