apiVersion: v1
kind: Service
metadata:
  name: session-archive
  labels: {app.kubernetes.io/name: session-archive, app.kubernetes.io/component: archive-controller}
spec:
  selector: {app.kubernetes.io/name: session-archive, app.kubernetes.io/component: archive-controller}
  ports:
    - {name: metrics, port: 9090, targetPort: metrics, protocol: TCP}
    - {name: auth-metrics, port: 9091, targetPort: auth-metrics, protocol: TCP}
