apiVersion: v1
kind: Service
metadata:
  name: runtime-controller
  labels: {app.kubernetes.io/name: runtime-controller}
spec:
  selector: {app.kubernetes.io/name: runtime-controller, app.kubernetes.io/component: hot-runtime}
  ports: [{name: technical, port: 9090, targetPort: technical, protocol: TCP}]
