apiVersion: v1
kind: Service
metadata:
  name: agent-runner-metrics
  labels: {app.kubernetes.io/name: agent-runner, app.kubernetes.io/component: job-runtime}
spec:
  clusterIP: None
  selector: {app.kubernetes.io/component: role-runtime, runtime.mattercodex.dev/managed: "true"}
  ports:
    - {name: metrics, port: 9090, targetPort: metrics}
