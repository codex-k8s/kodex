apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: internal-rpc-authority-restore-anchor-forward-only
spec:
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
      - apiGroups: [""]
        apiVersions: [v1]
        operations: [UPDATE, DELETE]
        resources: [secrets]
  matchConditions:
    - name: exact-resource
      expression: >-
        oldObject.metadata.namespace == 'mattercodex-system' &&
        oldObject.metadata.name == 'internal-rpc-authority-restore-evidence'
  validations:
    - expression: request.operation == 'UPDATE'
      message: restore evidence deletion is forbidden
    - expression: >-
        request.userInfo.username ==
        'system:serviceaccount:mattercodex-system:internal-rpc-authority-restore-pitr'
      message: only the independent PITR executor may update restore evidence
    - expression: >-
        int(object.metadata.annotations['mattercodex.dev/restore-anchor-revision']) ==
        int(oldObject.metadata.annotations['mattercodex.dev/restore-anchor-revision']) + 1
      message: restore evidence anchor revision must advance exactly once
    - expression: >-
        int(object.metadata.annotations['mattercodex.dev/restore-epoch']) >=
        int(oldObject.metadata.annotations['mattercodex.dev/restore-epoch'])
      message: restore epoch rollback is forbidden
    - expression: >-
        object.metadata.annotations['mattercodex.dev/restore-predecessor-revision'] ==
        oldObject.metadata.annotations['mattercodex.dev/restore-anchor-revision'] &&
        object.metadata.annotations['mattercodex.dev/restore-predecessor-digest-sha256'] ==
        oldObject.metadata.annotations['mattercodex.dev/restore-evidence-digest-sha256']
      message: restore evidence predecessor must equal served state
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: internal-rpc-authority-restore-anchor-forward-only
spec:
  policyName: internal-rpc-authority-restore-anchor-forward-only
  validationActions: [Deny]
  matchResources:
    namespaceSelector:
      matchLabels:
        kubernetes.io/metadata.name: mattercodex-system
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: internal-rpc-authority-restore-pitr-cluster-owner
spec:
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
      - apiGroups: [postgresql.cnpg.io]
        apiVersions: [v1]
        operations: [CREATE]
        resources: [clusters]
  validations:
    - expression: >-
        request.userInfo.username ==
        'system:serviceaccount:mattercodex-system:internal-rpc-authority-restore-pitr'
      message: only the independent PITR executor may create restore clusters
    - expression: >-
        object.metadata.namespace == 'mattercodex-system' &&
        object.metadata.name.startsWith('internal-rpc-authority-restore-') &&
        'mattercodex.dev/restore-id' in object.metadata.annotations &&
        'mattercodex.dev/restore-epoch' in object.metadata.annotations &&
        'mattercodex.dev/restore-intent-digest-sha256' in object.metadata.annotations
      message: PITR cluster must carry the exact restore intent projection
    - expression: >-
        object.spec.instances == 3 &&
        object.spec.bootstrap.recovery.source == 'origin' &&
        object.spec.bootstrap.recovery.recoveryTarget.targetTLI == 'latest' &&
        object.spec.imageName.contains('@sha256:')
      message: PITR cluster recovery and image boundary rejected
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: internal-rpc-authority-restore-pitr-cluster-owner
spec:
  policyName: internal-rpc-authority-restore-pitr-cluster-owner
  validationActions: [Deny]
  matchResources:
    namespaceSelector:
      matchLabels:
        kubernetes.io/metadata.name: mattercodex-system
