apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - podmonitor.yaml
patches:
  - path: verifier-patch.yaml
    target:
      group: apps
      version: v1
      kind: Deployment
      name: control-plane
