apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - podmonitor.yaml
  - networkpolicy.yaml
patches:
  - path: issuer-patch.yaml
    target:
      group: apps
      version: v1
      kind: Deployment
      name: control-api-gateway
