# Рабочие сущности провайдера

## Назначение

Домен описывает работу с нативными рабочими сущностями провайдера: `Issue`, `PR/MR`, комментариями, mentions, relationships, ветками, тегами, type, labels, milestones, полями проекта, метаданными провайдера, platform watermarks и открытыми инструкциями в body/comment слоях.

Сервис-владелец домена — `provider-hub`.

## Ключевые решения

- GitHub/GitLab остаётся источником рабочих артефактов.
- Инициатива — это `Issue` типа `initiative`, а не отдельная сущность платформы.
- Follow-up работа — это такие же `Issue` с другими типами.
- Платформа управляет оркестрацией, политиками, аудитом, приёмкой и проекциями вокруг нативных сущностей провайдера.
- `provider-hub` хранит нормализованные проекции, входящий журнал webhook, курсоры сверки, операционное состояние внешних аккаунтов у провайдера, лимиты и след provider-операций.
- `provider-hub` не владеет проектами, репозиториями как проектными сущностями, flow, ролями, слотами, заданиями, уведомлениями, пакетами и политикой внешних аккаунтов.
- Webhook — быстрый вход, но не гарантия; обязательна incremental reconciliation.
- Ручная синхронизация допустима только как админская постановка reconciliation job в очередь.

## Документы домена

| Документ | Путь |
|---|---|
| Требования | `product/requirements.md` |
| Дизайн | `architecture/design.md` |
| Модель данных | `architecture/data_model.md` |
| API-контракт | `architecture/api_contract.md` |
| План поставки | `delivery/provider_hub_delivery.md` |

## Карта Issue

- Доменная карта: `docs/delivery/issue-map/domains/provider-native-work-items.md`.
