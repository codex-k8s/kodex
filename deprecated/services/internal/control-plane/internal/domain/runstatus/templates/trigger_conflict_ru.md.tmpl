### ⚠️ Конфликт trigger-лейблов

Добавлен лейбл `{{ .TriggerLabel }}`, но на Issue уже есть другие trigger-лейблы из каталога `run:*`.

Конфликтующие лейблы:
{{- range .ConflictingLabels }}
- `{{ . }}`
{{- end }}

Что сделать:
1. Снимите конфликтующие лейблы.
2. Оставьте только один валидный trigger-лейбл для текущего этапа.
3. Повторно запустите этап, добавив нужный `run:*` лейбл на Issue (или PR, если запуск идёт из PR-контекста).
